"""Pi Dashboard — admin UI framework (FREE forever, ecosystem hub).

Backend surface is minimal; it mostly hosts license + updates for the
dashboard webapp itself. Pro add-ons (branding, audit log) may add more
endpoints here later.

Frontend webapp source: wp-content/pi-dashboard-webapp/
Runtime: served from app.pi-ecosystem.com, embedded into WP admin via
the pi-api plugin's iframe (wp-content/plugins/pi-api/).
"""
